import type { Lang } from "@/SysCore/i18n/lang";

const HomePage = (props: { lang: Lang; }) => (
    <main>
        <div className="container">Default HomePage</div>
    </main>
);
export default HomePage;
